<?xml version="1.0" encoding="utf-8"?>
<!-- Fragment Messages: Meta Messenger Style Disaster Mesh Communications -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_black"
    android:orientation="vertical"
    android:paddingStart="12dp"
    android:paddingTop="12dp"
    android:paddingEnd="12dp"
    android:paddingBottom="8dp">

    <!-- ========== SECTION 1: ACTIVE PEOPLE NEARBY (MESSENGER HORIZONTAL BAR) ========== -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvActiveSectionLabel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="monospace"
            android:text="@string/msg_active_section"
            android:textColor="@color/text_gray"
            android:textSize="10sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvActiveOnlineCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:textColor="@color/accent_green"
            android:textSize="9sp"
            android:textStyle="bold"
            tools:text="[ 4 ONLINE ]" />

    </LinearLayout>

    <!-- Horizontal Active Contacts List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvActiveContacts"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:clipToPadding="false"
        android:orientation="horizontal"
        android:scrollbars="none"
        tools:itemCount="5"
        tools:listitem="@layout/item_online_contact" />

    <!-- Divider Bar -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="10dp"
        android:background="@color/border_dark" />

    <!-- ========== SECTION 2: CONVERSATIONS & BROADCAST CHANNELS ========== -->
    <TextView
        android:id="@+id/tvConversationsSectionLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:text="@string/msg_conversations_section"
        android:textColor="@color/text_gray"
        android:textSize="10sp"
        android:textStyle="bold" />

    <!-- Vertical Conversations List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvConversations"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="6dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:scrollbars="vertical"
        tools:itemCount="4"
        tools:listitem="@layout/item_conversation" />

</LinearLayout>